# Copy this file to .env.local and edit the values.
# Vite only exposes vars prefixed with VITE_ to the client.

# Backend FastAPI server URL.
# Default works when you run `python server.py` (port 8000) on the same machine.
VITE_API_URL=http://localhost:8000
